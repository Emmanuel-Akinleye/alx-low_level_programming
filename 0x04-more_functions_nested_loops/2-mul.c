#include "main.h"
/**
 * mul - This is a functions that multiples two integers
 * @a: This is the first integer we'll use for mul
 * @b: This is the second integer we'll use for mul
 * Return: We'll return result
*/
int mul(int a, int b)
{
int result = a * b;
return (result);
}
