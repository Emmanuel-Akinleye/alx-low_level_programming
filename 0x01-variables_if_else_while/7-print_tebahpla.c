#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success1)
 */
int main(void)
{
char alpha;
for (alpha = 'z'; alpha >= 'a'; alpha--) 
{
putchar(alpha);
}
return (0);
}
